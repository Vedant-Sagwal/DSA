#include <iostream>
#include <vector>
using namespace std;

//activity selection
int findPlatform(vector<int>& arr, vector<int>& dep) {
    int count = 0;
    sort(dep.begin(), dep.end());

}

int main() {
    return 0;
}
