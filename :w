#include<iostream>
#include <vector>
using namespace std;

void n_queen_placement(int n, int row) {
    if (row == n) {
        return x; 
    }
    // i => columns
    for (int i = 0; i < n; i++) {
        
    }
}

int main() {
    return 0;
}
