#include <iostream>
#include <vector>
#include <unordered_map>
#include <queue>
using namespace std;

struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
};

bool sum_tree(TreeNode* root, int target) {
    //base case
    if (root == NULL) {
        return false;
    }
}

int main() {
    return 0;
}
